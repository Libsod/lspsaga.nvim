Для коммита 3 (chore(debug): add commented debug print statements):

1. В функцию hover:clean() добавить:
-- print("--- ENTERING hover:clean ---")

2. В функцию hover:do_request(args) добавить:
-- print("--- ENTERING do_request ---") -- <<< ДОБАВИТЬ
-- print("self.bufnr at start of do_request:", vim.inspect(self.bufnr)) -- <<< ДОБАВИТЬ

3. В функцию lsp.buf_request добавить:
-- print("--- LSP Callback Entered ---")

4. Перед проверкой self.bufnr добавить:
-- print('--- LSPSAGA RAW CONTENT ---')
-- print(vim.inspect(content))

5. В блоке if self.bufnr ... then добавить:
-- print("--- TAKING UPDATE PATH in do_request ---")

6. В функцию make_floating_popup_options в window.lua добавить:
-- print('--- window.lua: make_floating_popup_options ENTER ---') -- <<< ДОБАВИТЬ
-- print('Input opts:', vim.inspect(opts))

7. В конце функции make_floating_popup_options добавить:
-- print('--- window.lua: make_floating_popup_options EXIT ---') -- <<< ДОБАВИТЬ
-- print('Calculated anchor:', vim.inspect(anchor)) -- <<< ДОБАВИТЬ
-- print('Calculated row, col:', vim.inspect(row), vim.inspect(col)) -- <<< ДОБАВИТЬ
-- print('Final height, width to be returned:', vim.inspect(opts.height), vim.inspect(opts.width)) -- <<< ДОБАВИТЬ

8. В функцию obj:setlines добавить:
-- print('--- window.lua: Forcing buffer clear for bufnr:', self.bufnr) -- Отладочный print